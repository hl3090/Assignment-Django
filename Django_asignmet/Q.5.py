"""
Q. What is a QuerySet? Write program to create a new Post object in database:
Ans. 
    A QuerySet in Django is a list of objects that can be manipulated in a variety of ways before being saved to the database. 
    It is essentially a way to retrieve and manipulate data from the database in a programmatic way.
"""

# Example
"""
def login(request):
    if request.POST:
        email = request.POST['email']
        password = request.POST['password']

    else:
       print("only page loaded")    
"""